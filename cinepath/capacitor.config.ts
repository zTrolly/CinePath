import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.cinepath',
  appName: 'cinepath',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
